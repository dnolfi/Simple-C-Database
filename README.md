# Simple-C-Database (SCDB)
A simple database management system written in C. Uses CLI for user commands.

Currently, SCDB supports creating/deleting databases, creating/deleting rows and columns, loading and saving databases from and to files, changing database and column names, among other features.

To run the project: 
Run the makefile script using 'make'

Then run main.c using './main'
